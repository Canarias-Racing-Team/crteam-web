---
const { id, className = "", title, subtitle } = Astro.props;
---

<section id={id} class={className}>
  {title && <h2 class="text-2xl font-bold text-center">{title}</h2>}
  {subtitle && <p class="text-center mt-4">{subtitle}</p>}
  <slot />
</section>
